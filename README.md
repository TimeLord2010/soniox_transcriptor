# soniox_transcriptor

A lightweight desktop application that runs silently in the background. The user can invoke it at any time by holding a configurable hotkey, regardless of the active application. When the hotkey is held, a discrete pop‑up appears and the app begins transcribing the user's audio input in real‑time, injecting the resulting text directly into the currently focused text box.
